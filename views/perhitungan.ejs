<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('partials/head'); %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>
<body>
    <%-include('partials/header'); %>

    <section class="section">
        <div class="container">

            <nav class="breadcrumb" aria-label="breadcrumbs">
                <ul>
                  <li>
                    <a href="/adm/dashboard">
                      <span class="icon is-small">
                        <i class="fas fa-home" aria-hidden="true"></i>
                      </span>
                      <span>Dashboard</span>
                    </a>
                  </li>
        
                  <li class="is-active">
                    <a href="#">
                      <span class="icon is-small">
                        <i class="fa-solid fa-calculator"></i>
                      </span>
                      <span>Perhitungan TOPSIS</span>
                    </a>
                  </li>
                </ul>
            </nav>
    
            <hr>
            <div class="mb-4">
                <a href="/adm/dashboard">
                    <button type="button" class="button is-warning is-small">Kembali</button>
                </a>
            </div>

            <style>
                /* Tambahkan transisi untuk animasi yang lebih halus */
                .card-content {
                    /* display: none; Awalnya disembunyikan */
                    overflow: hidden;
                    transition: max-height 0.3s ease;
                }

                /*
                .card-content.is-active {
                    display: block; Ditampilkan saat active 
                    max-height: 500px;  Sesuaikan dengan tinggi konten 
                }
                */
                
            </style>


           <!-- Matriks Keputusan -->
           <div class="card">
                <header class="card-header">
                    <p class="card-header-title">Matriks Keputusan</p>
                    <button class="card-header-icon" aria-label="more options" data-target="decision-matrix">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="decision-matrix">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Matriks Keputusan akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>

            <!-- Matriks Ternormalisasi -->
            <div class="card mt-4">
                <header class="card-header">
                    <p class="card-header-title">Matriks Ternormalisasi</p>
                    <button class="card-header-icon" aria-label="more options" data-target="normalized-matrix">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="normalized-matrix">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Matriks Ternormalisasi akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>

            <!-- Matriks Ternormalisasi Terbobot -->
            <div class="card mt-4">
                <header class="card-header">
                    <p class="card-header-title">Matriks Ternormalisasi Terbobot</p>
                    <button class="card-header-icon" aria-label="more options" data-target="weighted-normalized-matrix">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="weighted-normalized-matrix">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Matriks Ternormalisasi Terbobot akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>

            <!-- Solusi Ideal Positif dan Negatif -->
            <div class="card mt-4">
                <header class="card-header">
                    <p class="card-header-title">Solusi Ideal Positif dan Negatif</p>
                    <button class="card-header-icon" aria-label="more options" data-target="ideal-solutions">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="ideal-solutions">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Solusi Ideal Positif dan Negatif akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>

            <!-- Jarak ke Solusi Ideal Positif dan Negatif -->
            <div class="card mt-4">
                <header class="card-header">
                    <p class="card-header-title">Jarak ke Solusi Ideal Positif dan Negatif</p>
                    <button class="card-header-icon" aria-label="more options" data-target="distance-to-ideal-solutions">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="distance-to-ideal-solutions">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Jarak ke Solusi Ideal Positif dan Negatif akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>

            <!-- Nilai Preferensi -->
            <div class="card mt-4">
                <header class="card-header">
                    <p class="card-header-title">Nilai Preferensi</p>
                    <button class="card-header-icon" aria-label="more options" data-target="preference-values">
                        <span class="icon">
                            <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                    </button>
                </header>
                <div class="card-content" id="preference-values">
                    <div class="content">
                        <!-- Contoh isi -->
                        <p>Nilai Preferensi akan ditampilkan di sini.</p>
                    </div>
                </div>
            </div>
            


            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    const cardHeaders = document.querySelectorAll('.card-header-icon');
            
                    cardHeaders.forEach(cardHeader => {
                        cardHeader.addEventListener('click', () => {
                            const targetId = cardHeader.dataset.target;
                            const targetElement = document.getElementById(targetId);
            
                            if (targetElement) {
                                targetElement.classList.toggle('is-active');
                            }
                        });
                    });
                });
            </script>
            

        </div>
    </section>
</body>
</html>